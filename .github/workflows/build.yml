# Reusable library build with explicit optional NuGet publication.
---
name: build

on:
  workflow_call:
    inputs:
      publish:
        type: boolean
        description: 'Publish the built package; failures and duplicates fail the job'
        required: false
        default: false
      version_override:
        type: string
        description: 'Version number override'
        required: false
      project_file:
        type: string
        description: 'Path to the .csproj file'
        required: true
      nuspec_file:
        type: string
        description: 'Path to the .nuspec file'
        required: true
    secrets:
      nuget_token:
        description: 'Nuget Api authentication token'
        required: false      

env:
  DOTNET_CONFIGURATION: 'Release'
  DOTNET_VERSION: '10.x'
  NUGET_SOURCE: 'https://api.nuget.org/v3/index.json'

jobs:
  build:
    name: ${{ inputs.publish && 'Build and Publish' || 'Build and Validate' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.nuget_token }}
          
      - name: Publication contract regressions
        run: python3 -m unittest discover -s scripts -p 'test_publish.py' -v

      - name: Get version
        id: get_version
        working-directory: 
        run: |
          inputVersion="${{ inputs.version_override }}"
          if [[ "${#inputVersion}" -gt 0 ]]
          then
            version="${inputVersion}"
          else
            version="$(grep -Eio -m 1 '<Version>.*</Version>' ${{ inputs.project_file }} | sed -r 's/<\/?Version>//g')"
          fi
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Run hermetic parser regressions
        timeout-minutes: 5
        run: |
          dotnet test tests/Mpeg4Lib.Test/Mpeg4Lib.Test/Mpeg4Lib.Test.csproj -c ${{ env.DOTNET_CONFIGURATION }} --filter "FullyQualifiedName!~TestSaveFile" --logger "trx;LogFileName=hermetic.trx" --results-directory artifacts/tests
          python3 scripts/check-hermetic-trx.py artifacts/tests/hermetic.trx \
            --require-class Mpeg4Lib.Test.ChapterQueueTests \
            --require-class Mpeg4Lib.Test.ChapterTextTests \
            --require-class Mpeg4Lib.Test.StszBoundaryTests \
            --require-class Mpeg4Lib.Test.WriterSampleTableTests \
            --require-class Mpeg4Lib.Test.AacOutputAdmissionTests \
            --require-class Mpeg4Lib.Test.AacPrerollTests \
            --require-class Mpeg4Lib.Test.EncoderMovieTimescaleTests \
            --require-class Mpeg4Lib.Test.SyncAdmissionTests \
            --require-class Mpeg4Lib.Test.FrameFilterFailureTests \
            --require-class Mpeg4Lib.Test.ChunkReaderFailureTests \
            --require-class Mpeg4Lib.Test.Ec3IndependentSubstreamTests

      - name: Retain hermetic test evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: hermetic-parser-tests
          path: artifacts/tests/
          if-no-files-found: error
          retention-days: 7

      - name: Build and Pack
        id: build_pack
        run: |
          nuspec_relative=$(realpath --relative-to="$(dirname ${{ inputs.project_file }})" "${{ inputs.nuspec_file }}")
          dotnet build ${{ inputs.project_file }} -c ${{ env.DOTNET_CONFIGURATION }}
          dotnet pack ${{ inputs.project_file }} --no-build -c ${{ env.DOTNET_CONFIGURATION }} -o . -p:NuspecFile="$nuspec_relative" -p:NuspecProperties="version=${{ steps.get_version.outputs.version }}"
          nupkg=$(ls *.nupkg)
          echo "nupkg=${nupkg}" >> "${GITHUB_OUTPUT}"
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: '${{ steps.build_pack.outputs.nupkg }}'
          path: ./${{ steps.build_pack.outputs.nupkg }}
          if-no-files-found: error
          retention-days: 7
          
      - name: Publish nupkg
        if: ${{ inputs.publish }}
        env:
          NUGET_API_KEY: ${{ secrets.nuget_token }}
          PACKAGE_PATH: ${{ steps.build_pack.outputs.nupkg }}
        run: bash scripts/publish-package.sh "$PACKAGE_PATH" "$NUGET_SOURCE"
              
